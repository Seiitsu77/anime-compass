# Offline training only. Do not add this file to the web application's
# production install. Use Linux/WSL: PyPI publishes LightFM 1.17 as source only,
# and native Windows binary/runtime behavior varies by toolchain.
lightfm==1.17
numpy>=1.26,<2
scipy>=1.11,<2
