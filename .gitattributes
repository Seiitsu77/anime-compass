# Model artifacts are parsed byte-for-byte. Git's CRLF normalisation silently
# corrupted a committed LightGBM booster once; these keep LF endings so a
# checkout on any platform still loads.
*.txt text eol=lf
data/evaluation/personalized/results/**/*.txt -text
*.npz binary
*.gz binary
*.sqlite binary
