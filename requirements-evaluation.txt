# Offline evaluation only. The web application never imports SciPy; the
# reference collaborative baselines (exact item-item cosine and implicit ALS)
# use it for sparse matrix algebra.
-r requirements-api.txt
scipy>=1.11,<2
# LambdaMART reranker training only. The serving path does not import it,
# and promoting the reranker would be the decision that changes that.
lightgbm>=4.5,<5
