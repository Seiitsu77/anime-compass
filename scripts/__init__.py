"""Operational entry points for data preparation, training, and evaluation.

`download_artifacts` is imported by the application at startup, so this
directory is a real package rather than a loose script folder.
"""
